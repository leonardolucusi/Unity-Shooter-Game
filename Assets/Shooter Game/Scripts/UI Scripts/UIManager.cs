using UnityEngine;

public class UIManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        UIUpgrade uIUpgrade = gameObject.GetComponentInChildren<UIUpgrade>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
